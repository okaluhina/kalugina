const Booking = require('../models/booking.model.js');

module.exports = {
  get: async (req, res) => {
    //валидация параметров?
    //paras? pagination
    //const companies = await Company.find().sort(req.params.sortBy);
    
    //res.send(companies);
  },
}
